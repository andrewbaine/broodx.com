// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Tap = require("tap");
var Point = require("./Point.bs.js");
var Origami = require("./Origami.bs.js");
var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Js_array = require("rescript/lib/js/js_array.js");

var paper = Origami.Model.make(undefined);

var unfolded = Origami.fold(undefined, paper, []);

var a = Point.make(0.0, 0.0);

var b = Point.make(1.0, 1.0);

var c = Point.make(1.0, 0.0);

var fold = [
  a,
  b,
  c
];

var expected_edges = unfolded.edges;

var expected_vertices = Js_array.map((function (p) {
        var paperLocation = p[0];
        if (Caml_obj.equal(paperLocation, a) || Caml_obj.equal(paperLocation, b) || Caml_obj.equal(paperLocation, c)) {
          return p;
        } else {
          return [
                  paperLocation,
                  c,
                  1
                ];
        }
      }), unfolded.vertices);

var expected_triangles = unfolded.triangles;

var expected = {
  edges: expected_edges,
  vertices: expected_vertices,
  triangles: expected_triangles
};

var actual = Origami.fold(undefined, paper, [fold]);

Tap.same(actual, expected);

var p = Point.make;

exports.p = p;
exports.paper = paper;
exports.unfolded = unfolded;
/* paper Not a pure module */
